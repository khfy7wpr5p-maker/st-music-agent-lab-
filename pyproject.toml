[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "st-music-agent-lab"
version = "0.1.0"
description = "Safety-first agent orchestration lab for ST music software"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
